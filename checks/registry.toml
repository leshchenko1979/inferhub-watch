[[checks]]
id = "core"
title = "Core probe: stream shape, tool call, Russian answer"
blurb = "One request asserts the OpenAI stream shape, a named report_answer tool call, and a clean Russian answer (text or tool argument). Any failure stops the route — nothing after it is probed."
severity = "fail"
contract = "openai-stream"
scores_rank = true

[[checks]]
id = "cache"
title = "Prompt cache on an identical repeat"
blurb = "The core payload is sent again byte-for-byte; pass requires cached_tokens > 0 on that first repeat."
severity = "fail"
contract = "openai-cache"
scores_rank = true