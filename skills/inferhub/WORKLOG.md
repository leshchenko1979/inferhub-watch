## Archived ledger entries (retention law: 10-entry cap)

# WORKLOG — task log for further analysis

**Owns:** every executed task (HQ process edits, dispatches, worker results) gets one line here: date, what, issue/topic, outcome. The log is the analysis substrate — no work without a log line.

| Date | Task | Issue / topic | Outcome |
|---|---|---|---|
| 2026-09-10 | Delegation law landed: HQ works on the process; workers via forum topics; work logging mandated | (this edit, process) | committed |
| 2026-09-10 | issue #2 cadence law implemented by worker | https://github.com/leshchenko1979/inferhub-watch/issues/2 | PROCESS.md rewritten to hourly cycle law (dedupe, quiet-cycle clause, ledger cycle timestamps); owner-seeded directive extended only; tests pass |
- 2026-09-10 06:0xZ HQ: surface law (grafana main) + object-link law landed, commit 8738266; cron prompt rewired. Pending dispatch: issue #2 (hourly cadence law, awaiting owner 👍).
- 2026-09-10 06:00Z HQ hourly cycle: sweep healthy (schedule 34323731790, 22.6h), Grafana 200 (main surface, probe data fresh), site 200 secondary, board 1 open (#2 pending 👍). Scan → issue #3 (step-3 sweep check must filter --event schedule; push runs mask missed sweeps).
- 2026-09-10 06:1xZ — Self-approval law received; HQ approved + executed issue #3 (step-3 sweep-health --event schedule filter), commit beef488, issue closed with receipt. No board items remain.
- 2026-09-10 06:16Z — cleanliness law (owner): hygiene audit run — 26 subagent branches (all merged) + gh-pages-fresh (stale 08-27 experiment, local-only) deleted, orphan worktree 6477167e removed, remote refs pruned, .ruff_cache/ gitignored. Law persisted in PROCESS.md §Cleanliness law.
- 2026-09-10 06:2xZ — Law: laws themselves are self-improvement objects (owner 06:22Z); scan step 6 amended, seed phrase immutable.
- 2026-09-10 06:3xZ: HQ — research-before-process law (5b65651) + SKILL.md format adoption: frontmatter added, PROCESS.md→SKILL.md rename, refs updated, hourly cron prompt rewired (912411e). Source: owner directive 06:25Z.
| 2026-09-10 06:3xZ | HQ | Cron execution law: hourly cron is now a thin session_notify trigger to HQ (359fe71b), HQ executes the cycle — skill 7e4228b, cron prompt rewired, test-fired |
- 2026-09-10 06:34Z: HQ hourly cycle — sweep healthy (schedule 34323731790, 23.1h), Grafana 200, board 0 open, tests 468 passed. Scan → issue #4 (step-5 Grafana check lacked probe-data-currency leg); self-approved + landed same cycle. Commit follows.

## Archived improvement-ledger entries (2026-09-10)

- 2026-09-10: skill created — initial process law (issues, dispatch, ontology, daily self-improvement).
- 2026-09-10: first daily cycle executed clean — no `hq:` proposal (no process gap found); bootstrap issue #1 closed with receipt.
- 2026-09-10: rule-extraction law landed (owner order) — extract and persist a rule from every conversation/incident; this ledger records extractions.
- 2026-09-10: self-improvement section reseeded (owner order) — directive = "find objects in the project for self-improvement"; procedure made self-evolving (scan one object per cycle, `hq:` issue per proposal, section grows, procedure itself a valid object).
- 2026-09-10: HQ cycle 05:5xZ — sweep healthy (scheduled run success 22.3h ago, within window; today's 02:17Z firing still pending/queued by GitHub), board 0 open, tests 468 passed. Self-improvement scan object: cron cadence mismatch — the PROCESS.md cycle law said "daily cron" but the owner retimed it to hourly (05:41Z); proposed `hq:` issue to align (pending owner 👍).
- 2026-09-10 07:1xZ — mission law landed (owner: "provide timely updates on routes with bigger IQ per projected price", commit 9cb81a2). First act: found route_metrics.iq NULL for all rows (slug-mapping defect) → issue #6 filed, self-approved, dispatched to worker topic "Worker — HQ cycles". Value table (discount vs official ask) reported to owner.
- 2026-09-10 09:15Z: [issue #7](https://github.com/leshchenko1979/inferhub-watch/issues/7) landed — grounded step 5(b) probe currency command in SKILL.md with verified apps docker container snippet.
- 2026-09-11 12:1xZ hourly cycle — sweep healthy ([run 34573828600](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34573828600), 07:19Z — the only **scheduled** run; the 10:26–11:31 runs are push events), Grafana 200 ok (dashboard data 11:24Z, sync cron healthy), Pages 200 ok, board 1 open at cycle start ([#18](https://github.com/leshchenko1979/inferhub-watch/issues/18), run-name fix landed — criterion 2 now verified in the wild, push runs render `Watch — site deploy`). Value: board top IQ/$ `ag/gemini-3.8-flash-high` (IQ 41.2, 79,968), then `cbcn/glm-5.3-flash` (IQ 41.9, 79,292). Scan object: **Class 7 Agent Workspaces** — the one class never scanned. Two mechanisms were assumed to exist and do not: (1) a `session_notify` brief to a freshly spawned worker **parks** instead of delivering — four first-hand receipts at `restart_recovery.rs:248` for `c34f5cbb` (#15), `45bac1d7` (#16 gate-rank), `5c5d7214` (#18), `0628f7d3` (#16 crown); (2) `session_bindings` rows are written **only by the inbound-message handler** (`channels/telegram/handler.rs` ~1890), never at topic creation or spawn time — topics 399/421/444 have **no** binding row although prior ledger entries assert those workers were "session bound". Also: worker sessions are titled `subagent: …` although the skill reserves sub-agents for reviews. Filed [issue #21](https://github.com/leshchenko1979/inferhub-watch/issues/21) (four findings; A–C are process edits, D is the code half), self-approved (premises verified first-hand: log receipts, a direct `sqlite3` read of `session_bindings`, session titles from the `sessions` table, and the `chat_id`-only SQL at `scripts/auto_route_switch.py:341`), created Telegram forum topic 510 (`Worker — #21 switcher topic scoping`), spawned worker `760c13cd` (`09372edd-bf9c-4461-82b4-3cbf057c66de`), **briefed via `send_input`** — `session_notify` would have parked, which is Finding A itself.
- 2026-09-10 10:07 UTC | Codified minimum charge floor invariant in ONTOLOGY.md | #8 | closed |
| 2026-09-10 10:14 UTC | Made Grafana dashboard public and resolved template variable no-data error | #9 | closed |
| 2026-09-10 11:08 UTC | Tracked canonical Grafana dashboard in repo dashboards/watch.json | #10 | closed |
| 2026-09-10 12:20 UTC | Added TPS column, TPS chart, blended effective $/Mtok stat card and timeseries | (dashboard) | closed |
| 2026-09-10 13:10 UTC | Self-improvement scan: filed & self-approved #11 (protect data/catalog.json from sync write); dispatched topic 257 | https://github.com/leshchenko1979/inferhub-watch/issues/11 | dispatched |
| 2026-09-10 17:15 UTC | HQ cycle — sweep healthy (run 34449518157), Grafana 200 (16:23Z), Pages 200. Reverted hallucinated daily cadence edit; restored hourly HQ trigger inferhub-watch-hq-hourly (0 * * * *). Tests 496 passed. | (cycle) | closed |
- 2026-09-10 06:00Z hourly cycle — sweep healthy (schedule run 34323731790 success 2026-09-09T07:25Z, 22.6h, within window; no 02:17Z firing for 09-10 yet — GitHub has not fired it, watch next cycle), Grafana 200 ok (probe data 2026-09-09T07:25 fresh), site 200 (same snapshot), board 1 open. Self-improvement scan object: step-3 sweep-health command returns push runs, masking a missed schedule run ��� proposed issue #3 (filter `--event schedule`), pending owner 👍.
- 2026-09-10: rule extraction — 3 owner orders (05:43–05:45Z) landed as "HQ role boundary" section: HQ on-process-not-in-process, all HQ work logged, worker topics via userbot tg_mtproto.
- 2026-09-11 07:5xZ — HQ hourly cycle: sweep healthy (schedule run 34573828600, 07:19Z), Grafana 200 (probe data 07:28Z), Pages 200, board 0 open, cleanliness clean. Scan object Class 3 Surfaces → filed & self-approved issue #15 (Grafana IQ/$ panels ungated projected vs board gated realized; same route 240,233 vs 82,400). Topic 399 created via userbot `messages.CreateForumTopic`; worker `b31e40b6` (`c34f5cbb-4baa-43c9-814e-7fab4b7d249b`) spawned and briefed via `session_notify`.
- 2026-09-11 09:0xZ — HQ hourly cycle: sweep healthy (schedule run 34573828600, 07:19Z), Grafana 200 (dashboard data 08:24Z), Pages 200, board 1 open (#16), cleanliness clean (main + gh-pages only, single worktree, tree clean). Value: board top IQ/$ `cbcn/glm-5.3-flash` (IQ 41.9, 83,800), then `ag/gemini-3.8-flash-high` (IQ 41.2, 82,400). Scan object Class 4 Schedulers & Triggers → filed & self-approved [issue #17](https://github.com/leshchenko1979/inferhub-watch/issues/17): the Class 4 inventory named 2 of 3 live scheduler jobs (`inferhub-usage-logs-sync` missing), and step 5(b) read `route_metrics.updated_at` as "probe data currency" although the hourly sync cron is its only writer — never the sweep, which has zero Postgres references. Landed in SKILL.md same cycle (HQ process edit).
- 2026-09-11 09:2xZ — HQ hourly cycle: sweep healthy (schedule run 34573828600, 07:19Z), Grafana 200 (dashboard data 08:24Z), Pages 200, board 2 open at start, cleanliness clean (main + gh-pages only, single worktree, tree clean). Closed #17 (both criteria verified first-hand). Value: board top IQ/$ `cbcn/glm-5.3-flash` (IQ 41.9, 83,800), then `ag/gemini-3.8-flash-high` (IQ 41.2, 82,400). Scan object Class 1 Repository & CI/CD → filed & self-approved [issue #18](https://github.com/leshchenko1979/inferhub-watch/issues/18): `watch.yml` has no `run-name`, so push runs (which skip the probe) and sweep runs are both `name=Watch` + `conclusion=success` — HQ's own step-3 read fell into that trap this cycle. Issue #3 fixed only the procedure; #18 is the structural half. Topic 444 created via userbot `messages.CreateForumTopic`; worker `9c8de03e` (`5c5d7214-9764-4b72-b837-d8de64e2d283`) spawned and briefed via `session_notify`. Topic hygiene: 399, 257, 288, 45 renamed `Done — …`.
- 2026-09-11 09:4xZ — owner ruling "1+3": drop the TOOLS.md carrier-dispatch note (done) + retire the Spearman rho criterion for a TOP-1 CROWN GATE at 15% tolerance. Owner also corrected HQ's process ("And why haven't you delegated this work?") — the gate re-tune is project work and was dispatched, not done inline. Ruling recorded on #16; topic 465 (`Worker — #16 top-1 crown gate`) created via userbot `messages.CreateForumTopic`; worker `4f72ca76` (`0628f7d3-4e4b-43f0-bc1b-1e984871fe7a`) spawned, session bound; brief delivered via `resume_agent` (the initial `session_notify` routed but did not wake the already-completed worker). Crown pinned from the code, not guessed: lowest projected $/M — the board's `_iq_sort_key` is `(eff_val, -(iqps))`, price primary with IQ/$ only a tiebreaker; estimator pinned to `probe.basis.projected`. Crown gate: per transition, does the crowned route realize within 15% of the true cheapest? 0% → 11/14 FAIL, 15% → 12/14 PASS, 60% → 13/14, 120% → 14/14. Topic hygiene: 421 renamed `Done — #16 gate rank fidelity`.
- 2026-09-11 10:0xZ — HQ hourly cycle: sweep healthy (schedule run 34573828600, 07:19Z — still the only scheduled run), Grafana 200 (dashboard data 09:25Z), Pages 200, board 2 open at start (#16 in flight, #18 landed), cleanliness clean (main + gh-pages only, single worktree). All three inferhub crons verified live. Value: board top IQ/$ `cbcn/glm-5.3-flash`, then `ag/gemini-3.8-flash-high`. Scan object **Class 2 Data & Storage** (the one class never scanned) → filed & self-approved [issue #19](https://github.com/leshchenko1979/inferhub-watch/issues/19): `probe/cache_ramp.py:104` hardcodes its output filename to `cache_ramp_20260905.json`, the only hardcoded output filename in the codebase, while every other dated artifact derives from the clock (`probe/run.py:208`, `probe/pricing.py:730`) — so a re-run on a later date overwrites a tracked record whose name asserts a date the payload does not have. Premises verified first-hand: sole hardcode; artifact git-tracked; no consumer reads it by name. Topic 476 created via userbot `messages.CreateForumTopic`; worker `2bb2e9b9` (`ad6f4b4a-0c26-4614-8621-815bd5016c8e`) spawned, session bound, briefed via `session_notify`. `data/runs/` growth examined and NOT proposed — load-bearing history for sweep-window reconstruction.

- 2026-09-10: delegation law landed (owner order: "work on the process, not in the process") — every task dispatched to a worker, worker spawn requires a forum topic (created via userbot `messages.CreateForumTopic`), all work logged (ledger + WORKLOG.md) for further analysis.
- 2026-09-11 ~10:1xZ — owner directive (three items): "Reload skill on using subagents. Add forum and topic to the project object classes list. Keep a ledger of object class checks." Landed in SKILL.md same turn: (1) new §Worker / sub-agent usage (the reload target) — the two mechanisms (topic worker for all implementation/bugfix/sync/research; unbound review sub-agent for reviews only), thin spawn + `session_notify` brief with a truthful ledger note when the `resume_agent` fallback path is used, the single-shared-checkout staging rule, and first-hand verification before closing an issue; (2) the object-class inventory extended 7 → 8 with **8 Forum & Topics** (the forum group `-1004379632866` and its topics as first-class objects); (3) a new **Object-class check ledger** recording which class each cycle examined (seeded 1–5; 6/7/8 next). Archived the oldest Improvement-ledger entry (2026-09-10 delegation law) here per the retention law. Same turn: #16 and #19 verified first-hand and closed — crown gate live at `pass=t | n=14 | land=12 | share=0.857 | tol=0.15 | min_n=10`, pytest 549 passed, unittest 540 OK, all four surfaces reworded.

## Archived improvement-ledger entries (2026-09-11)

- 2026-09-10 13:10Z hourly cycle — sweep healthy ([run 34449518157](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34449518157)), Grafana 200 ok (probe currency 12:23Z), Pages 200 ok. Board: 1 open ([issue #11](https://github.com/leshchenko1979/inferhub-watch/issues/11)). Scan object: Class 5 Code & Config — `scripts/sync_usage_logs.py` catalog write path dirties tracked `data/catalog.json`. Filed [issue #11](https://github.com/leshchenko1979/inferhub-watch/issues/11), self-approved, created Telegram forum topic 257 (Worker — #11 Cleanliness & Sync), dispatched. Archived from the Improvement ledger 2026-09-11 11:0xZ per the retention law.

- 2026-09-11 11:0xZ — HQ hourly cycle: sweep healthy (schedule run 34573828600, 07:19Z), Grafana 200 (dashboard data 10:24Z), Pages 200, board 1 open (#18, awaiting the 02:17Z sweep), cleanliness clean (main + gh-pages only, single worktree, tree clean). Scan object **Class 6 Process & Vocabulary** → filed & self-approved [issue #20](https://github.com/leshchenko1979/inferhub-watch/issues/20): the term **crown** is load-bearing (74 occurrences across 17 tracked files — probe, site, dashboards, tests, skills) yet has no `ONTOLOGY.md` entity row (prose inside the Projection-gate definition only), and the **Board** row still asserted an unconditional "realized $/M ascending" sort although the basis is gated (`site/board.py:243` `use_proj = gate["pass"]`; `site/ticket.py:37` same) and the gate now passes. Landed in ONTOLOGY.md same cycle (HQ process edit), commit `5ea9380`. Also recorded: the owner's three-part directive of 10:13Z landed at 10:26Z but the landing turn ended with **no channel message** — the owner learned it from the 11:0xZ cycle. Rule extracted into SKILL.md §HQ standing duties item 4.
- 2026-09-10 18:00Z hourly cycle — sweep healthy ([run 34449518157](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34449518157)), Grafana 200 ok (probe data 17:23Z), Pages 200 ok. Board: 1 open ([issue #13](https://github.com/leshchenko1979/inferhub-watch/issues/13)). Top IQ/$ route: `zai/glm-5.3-flash` (IQ 41.9, 272,964 IQ/$), followed by `ag/gemini-3.8-flash-high` (52,820 IQ/$). Scan object: Class 5 Code & Config — `scripts/auto_route_switch.py` lacks fallback to `keys.toml` for `bot_token`. Filed [issue #13](https://github.com/leshchenko1979/inferhub-watch/issues/13), self-approved, created Telegram forum topic 343 (`Worker — #13 keys.toml Fallback`), bound session and dispatched via session_notify.

- ARCHIVED 2026-09-11 12:1xZ per the retention law.
- 2026-09-10 ~18:4xZ owner directive — owner ordered the switcher's alert path moved off raw Bot API: "It should use opencrabs session notify instead" (supersedes [issue #13](https://github.com/leshchenko1979/inferhub-watch/issues/13) token machinery; #13 was already resolved by commit [ee55843](https://github.com/leshchenko1979/inferhub-watch/commit/ee55843)). Filed [issue #14](https://github.com/leshchenko1979/inferhub-watch/issues/14), created Telegram forum topic 357 (`Worker — #14 session_notify alerts`), spawned worker `b0864cce` (`3e589b77-f469-4fda-a0e4-96b074c17630`). Landed in [0a4b750](https://github.com/leshchenko1979/inferhub-watch/commit/0a4b7504386f5f96424a27edc61d37825595d06c): deleted `get_bot_token` / `send_telegram_notification`, added dynamic target resolution (`notify_session` override → `session_bindings` active group session → fallback) and receipt-checked `opencrabs session notify --confirm`. 512 tests passed, [issue #14](https://github.com/leshchenko1979/inferhub-watch/issues/14) closed.- 2026-09-11 07:5xZ hourly cycle — sweep healthy ([run 34573828600](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34573828600), 07:19Z), Grafana 200 ok (probe data 07:28Z), Pages 200 ok, board 0 open at cycle start, cleanliness clean (main + gh-pages only, single worktree). Value: board top IQ/$ `cbcn/glm-5.3-flash` (IQ 41.9, 83,800), then `ag/gemini-3.8-flash-high` (82,400). Scan object: Class 3 Surfaces — the four Grafana IQ/$ panels use an **ungated** projected basis while the board gates projected behind `projection_gate` (closed, 44/109 within 20%), so the same route reads 240,233 on Grafana vs 82,400 on the board. Filed [issue #15](https://github.com/leshchenko1979/inferhub-watch/issues/15), self-approved (premises verified first-hand), created Telegram forum topic 399 (`Worker — #15 IQ/$ basis alignment`), spawned worker `b31e40b6` (`c34f5cbb-4baa-43c9-814e-7fab4b7d249b`), briefed via `session_notify`. Canonical-basis choice left to the owner (mission says "projected", gate says not yet earned).

- ARCHIVED 2026-09-11 13:0xZ per the retention law.
- 2026-09-11 08:2xZ owner directive — owner ruled **projected canonical** ("re-tune the gate so both surfaces use it"), settling the #15 canonical-basis question. HQ measured before touching anything: `projection_gate` scores absolute deviation, but ONTOLOGY.md line 39 defines its job as certifying the IQ/$ **ranking** — median rho 0.918, rho>=0.5 on 14/14, yet rho>=0.8 on only 9/14 = 0.643, and every rank row also fails `GATE_MIN_N=20` because a rank gate's unit is the transition (n=14), not the route-pair (n=109). No constant tweak passes honestly (tol would need 63.5%; the projection is unbiased at median ratio 0.9487; the board's own smoothed estimator scores worse at 0.303). Closed [issue #15](https://github.com/leshchenko1979/inferhub-watch/issues/15) — its divergence defect is fixed in [18d9f1e](https://github.com/leshchenko1979/inferhub-watch/commit/18d9f1e6df741f63b1329a795afd0ef64a073133), verified first-hand by HQ (gate `f|109|44|0.404|0.2`, 17 route bases, 537 tests, HEAD == origin/main). Filed [issue #16](https://github.com/leshchenko1979/inferhub-watch/issues/16) (gate re-tune onto rank fidelity), self-approved, created topic 421, spawned worker `1e7660d6` (`45bac1d7-59b9-4a56-b698-ac0fdf8081ed`), briefed via `session_notify`. The rho bar is the owner's to set and is NOT to be tuned into a pass.
- 2026-09-11 09:0xZ hourly cycle — sweep healthy ([run 34573828600](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34573828600), 07:19Z), Grafana 200 ok (dashboard data 08:24Z), Pages 200 ok, board 1 open ([issue #16](https://github.com/leshchenko1979/inferhub-watch/issues/16), gate re-tune, awaiting the owner's rho bar). Value: board top IQ/$ `cbcn/glm-5.3-flash` (IQ 41.9, 83,800), then `ag/gemini-3.8-flash-high` (IQ 41.2, 82,400). Scan object: Class 4 Schedulers & Triggers — the Class 4 inventory named 2 of 3 live jobs (missing `inferhub-usage-logs-sync`), and cycle step 5(b) read `route_metrics.updated_at` as "probe data currency" when that table is written hourly by the sync cron and never by the sweep (so the leg could not detect a missed sweep, and mis-routed staleness to the sweep). Filed [issue #17](https://github.com/leshchenko1979/inferhub-watch/issues/17), self-approved, landed in this file same cycle.

- 2026-09-11 13:0xZ hourly cycle — sweep healthy ([run 34573828600](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34573828600), 07:19Z — the only **scheduled** run; the 10:26–12:31 runs are push events, all rendering `Watch — site deploy`), Grafana 200 ok, dashboard data 12:24Z (sync cron healthy, `inferhub-usage-logs-sync` last run 12:23Z), Pages 200 ok, all three inferhub crons live (`inferhub-watch-hq-hourly` 13:00Z, `inferhub-usage-logs-sync` 12:23Z, `inferhub-daily-report` 05:00Z), board 2 open at cycle start ([#18](https://github.com/leshchenko1979/inferhub-watch/issues/18) awaiting the 02:17Z sweep for criterion 3; [#21](https://github.com/leshchenko1979/inferhub-watch/issues/21) criteria 1–2 verified, 3 partial, 4 open), cleanliness clean (main + gh-pages only, single worktree, tree clean). Value: board top IQ/$ `ag/gemini-3.8-flash-high` (IQ 41.2, 79,968), then `cbcn/glm-5.3-flash` (IQ 41.9, 79,292). Scan object: **Class 5 Code & Config** (oldest unscanned, 2026-09-10 18:00Z) — the premise **"the crown is what the switcher acts on"** is asserted three times (`ONTOLOGY.md:16` Crown row, `ONTOLOGY.md:40` Projection-gate row, `probe/official_compare.py` `projection_gate` docstring) and is **false in code**: `scripts/auto_route_switch.py::calculate_value` ranks on `IQ / (0.99·ask_in + 0.01·ask_out)` with asks taken from the **catalog floor** (`probe/catalog.py:56` `min(points_in)`), no cache discount, and `grep -n -i "crown\|board" scripts/auto_route_switch.py` returns **no matches**; `RouteCandidate.supports_cache` is populated and never read. The formula is **owner-specified in [#12](https://github.com/leshchenko1979/inferhub-watch/issues/12)**, so the switcher is not the defect — the claim is. First-hand, both rankings computed from committed data through their own production entry points: switcher #1 `cb/deepseek-v4.1-flash` (Value 255,663.4) is **absent from the board entirely** (no billed traffic — ghost-route law) yet is the live `[providers.custom.inferhub].default_model`; board crown `ag/gemini-3.8-flash-high` (basis 0.000515) ranks **#2** in the switcher; same-route basis ratio 0.85×–8.92× (the bases cross over, so neither ordering dominates). Filed [issue #22](https://github.com/leshchenko1979/inferhub-watch/issues/22), self-approved for the **finding** only — the edit direction is deliberately NOT chosen: correcting the three statements to match the code is mechanical, but changing the switcher's basis changes **which route the fleet runs on** (spend/behaviour) and is the owner's ruling under the self-approval guardrail. No worker dispatched while the direction is open (nothing a worker could correctly implement yet); the doc edit is held until the ruling, since the two possible rulings imply opposite edits. Surfaced in the cycle report as the missing half of the owner's live TPS-ranking discussion — the board's ordering is what he is discussing, and the ranking the fleet actually acts on is a **second** one. Oldest ledger entry (2026-09-11 07:5xZ) archived to WORKLOG.md per the retention law.

- 2026-09-11 11:2xZ owner ruling — owner left the class-8 question to HQ ("Object class 8 - we may reclassify to the surfaces group if needed - you decide"): **folded class 8 Forum & Topics into class 3 Surfaces** — the forum and its topics are where the owner OBSERVES the project (a surface), while the lanes themselves are sessions (class 7). Inventory back to **7 classes**; forum + topics stay ON the list, now inside class 3. Same edit: the object-class check ledger's class-3 row records the reclassification, and the class-8 row is removed with a footnote explaining where it went. Oldest ledger entry archived to WORKLOG.md per the retention law.
- 2026-09-11 12:1xZ hourly cycle — sweep healthy ([run 34573828600](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34573828600), 07:19Z — the only **scheduled** run; the 10:26–11:31 runs are push events), Grafana 200 ok (dashboard data 11:24Z, sync cron healthy), Pages 200 ok, board 1 open at cycle start ([#18](https://github.com/leshchenko1979/inferhub-watch/issues/18), run-name fix landed — criterion 2 now verified in the wild, push runs render `Watch — site deploy`). Value: board top IQ/$ `ag/gemini-3.8-flash-high` (IQ 41.2, 79,968), then `cbcn/glm-5.3-flash` (IQ 41.9, 79,292). Scan object: **Class 7 Agent Workspaces** — the one class never scanned. Two mechanisms were assumed to exist and do not: (1) a `session_notify` brief to a freshly spawned worker **parks** instead of delivering — four first-hand receipts at `restart_recovery.rs:248` for `c34f5cbb` (#15), `45bac1d7` (#16 gate-rank), `5c5d7214` (#18), `0628f7d3` (#16 crown); (2) `session_bindings` rows are written **only by the inbound-message handler** (`channels/telegram/handler.rs` ~1890), never at topic creation or spawn time — topics 399/421/444 have **no** binding row although prior ledger entries assert those workers were "session bound". Also: worker sessions are titled `subagent: …` although the skill reserves sub-agents for reviews. Filed [issue #21](https://github.com/leshchenko1979/inferhub-watch/issues/21) (four findings; A–C are process edits, D is the code half), self-approved (premises verified first-hand: log receipts, a direct `sqlite3` read of `session_bindings`, session titles from the `sessions` table, and the `chat_id`-only SQL at `scripts/auto_route_switch.py:341`), created Telegram forum topic 510 (`Worker — #21 switcher topic scoping`), spawned worker `760c13cd` (`09372edd-bf9c-4461-82b4-3cbf057c66de`), **briefed via `send_input`** — `session_notify` would have parked, which is Finding A itself. Oldest ledger entry (2026-09-10 ~18:4xZ) archived to WORKLOG.md per the retention law. **Outcome (same cycle):** the worker landed the code half in `b9674e8` (both binding legs scoped to `thread_id`, a newer worker-topic binding can no longer win, fail-loud when HQ is unbound); HQ re-ran both suites on the committed tree (540 unittest OK, 551 pytest passed) and probed the live resolver against the real DB — it returns the HQ session, MATCH. The live defect was latent (HQ held the newest binding), so the synthetic tests carry the proof. **#21 stays open:** criterion 4 (worker sessions titled `subagent: …`) is untouched — that string is set by OpenCrabs core (`src/brain/tools/subagent/spawn.rs:352`), so it needs either a core change or a project-side rename, and the mechanism is unverified — and criterion 3 is partial. Do not re-file; verify a mechanism first.
- 2026-09-11 11:0xZ hourly cycle — sweep healthy ([run 34573828600](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34573828600), 07:19Z), Grafana 200 ok (dashboard data 10:24Z), Pages 200 ok, board 1 open ([#18](https://github.com/leshchenko1979/inferhub-watch/issues/18), run-name fix landed, awaiting the 02:17Z sweep), cleanliness clean (main + gh-pages only, single worktree, tree clean). Scan object: **Class 6 Process & Vocabulary** — the vocabulary gained the term **crown** with the #16 landing (68 occurrences across 14 tracked files — `git grep -I -i crown`: `ONTOLOGY.md`, `dashboards/watch.json`, `probe/basis.py`, `probe/official_compare.py`, `probe/pgstore.py`, `scripts/sync_usage_logs.py`, `site/board.py`, `site/docs.md`, `site/ticket.py`, `tests/test_official.py`, `tests/test_pgstore.py`, `tests/test_wiring.py`, `skills/inferhub/SKILL.md`, `skills/inferhub/WORKLOG.md`) yet `ONTOLOGY.md` — whose own header declares it the "single source of truth for domain vocabulary" — had **no Crown row** (prose inside the Projection-gate definition only), and its **Board** row still asserted an unconditional "realized $/M ascending" sort although the basis is gated (`site/board.py:243` `use_proj = gate["pass"]`; `site/ticket.py:37` same) and the gate now passes. Filed [issue #20](https://github.com/leshchenko1979/inferhub-watch/issues/20), self-approved (premises verified first-hand), landed in ONTOLOGY.md same cycle — [5ea9380](https://github.com/leshchenko1979/inferhub-watch/commit/5ea9380086d0626ab50b4ee901d8f7684f8bb9b9); `pytest` 549 passed, `unittest` 540 OK. Oldest entry (2026-09-10 13:10Z) archived to WORKLOG.md per the retention law. Also extracted: the 10:26Z landing turn for the owner's three-part directive committed and renamed topics but sent **no channel message** — the owner learned it only from this cycle; the rule is now §HQ standing duties item 4.
- 2026-09-11 09:4xZ owner ruling "1+3" — owner picked option 1 (drop the TOOLS.md carrier-dispatch note) and option 3 (retire the Spearman rho criterion; the projection gate becomes a **TOP-1 CROWN GATE at 15% tolerance**) from HQ's 3-option keyboard, then corrected HQ's process: "And why haven't you delegated this work?" Option 3 is project work, so it was **dispatched, not done inline** — ruling recorded on [issue #16](https://github.com/leshchenko1979/inferhub-watch/issues/16#issuecomment-5632703722), Telegram forum topic 465 (`Worker — #16 top-1 crown gate`) created via userbot `messages.CreateForumTopic`, worker `4f72ca76` (`0628f7d3-4e4b-43f0-bc1b-1e984871fe7a`) spawned, session bound in `session_bindings`; the substantive brief followed via `resume_agent` (the initial `session_notify` routed but did not wake the worker, which had already completed a reconnaissance-only turn — the spawn prompt is only the thin seed). Crown definition pinned from the code, not guessed: the crown is the **lowest projected $/M**, because the board's `_iq_sort_key` returns `(eff_val, -(iqps))` — price PRIMARY, IQ/$ only a tiebreaker within an equal cost band; the switcher's own ordering is IQ/$ descending, a *different* crown, recorded as a docstring note with the switcher untouched. Estimator pinned to `probe.basis.projected` (the single money-basis owner). The crown gate: per transition, does the route the board would have crowned realize a cost within 15% of the true cheapest? 0% → 11/14 FAIL, **15% → 12/14 PASS**, 60% → 13/14, 120% → 14/14; 15% is the switcher's own `SWITCH_THRESHOLD`. Retires `_spearman` + `GATE_RHO_BAR`; the rho row `pass=f | n=14 | land=9 | share=0.643 | bar=0.8 | min_n=10 | rho_median=0.9` stands until the worker lands. Option 1 executed: the TOOLS.md note dropped. Topic hygiene: 421 renamed `Done — #16 gate rank fidelity`.

**Archived 2026-09-12 08:1xZ** (evicted by the criterion-2 landing; retention law 10-entry cap):
- 2026-09-12 03:0xZ hourly cycle (post-gap backfill begins here — cycles 14:00Z→02:00Z produced reports but no ledger entries during the cron rc=127 window; their receipts live in the HQ topic reports) — cycle continuity clean (double-trigger deduped, single 03:00Z run). Sweep check: **scheduled sweep did NOT fire at 02:17Z** — last scheduled run remains 34573828600 (Sep 11 07:19Z); the schedule cron moved to `17 2 * * *` in a185248 (Sep 8) yet observed firings are 07:25/07:21/07:19Z on Sep 9/10/11 = **consistent ~5h delay, 3 days of evidence** (GitHub scheduler degradation, external). #18 criterion 3 gate re-based to ~07:19Z today (its last scheduled run predates the 09:32Z run-name commit). Grafana 24/24 panels valid after worker fixes `37227ca`+`0f05e5b`; Pages 200; route_metrics 02:24Z. Owner orders executed same-turn: **#25 filed** (hourly full-catalog price query — "query prices hourly, the entire catalogue") and **dispatched to persistent lane `1122b15e`** (topic 559 — live roster read; the 00474767 id I used earlier is NOT in the session roster; identifier-law catch) via `session_notify` mode=now with deliver receipt. TPS staleness verified FIXED in code first-hand: `auto_route_switch.py:722-730` overlays live Postgres perf_stats onto the payload — DeepSeek 4.1 resolves TPS=240.1 [prod] (5,572 samples) while `data/pricing.json` still has `{}` for it. Ledger stamped: Class 4 **DEGRADED** (scheduler delay), Class 2 IMPROVING. Scan object: Class 4 Schedulers & Triggers. Oldest ledger entry (2026-09-11 08:2xZ owner directive) archived to WORKLOG.md per the retention law.
- 2026-09-11 10:0xZ hourly cycle — sweep healthy ([run 34573828600](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34573828600), 07:19Z — still the only **scheduled** run; the 09:33–09:59 runs are push events), Grafana 200 ok (dashboard data 09:25Z, sync cron healthy), Pages 200 ok, board 2 open at cycle start ([#16](https://github.com/leshchenko1979/inferhub-watch/issues/16) crown-gate re-tune in flight; [#18](https://github.com/leshchenko1979/inferhub-watch/issues/18) run-name fix landed, awaiting the 02:17Z sweep). All three inferhub crons verified live in the `ops` profile (`inferhub-daily-report` 05:00Z, `inferhub-usage-logs-sync` 09:23Z, `inferhub-watch-hq-hourly` 10:00Z). Value: board top IQ/$ `cbcn/glm-5.3-flash`, then `ag/gemini-3.8-flash-high`. Scan object: **Class 2 Data & Storage** — the one class never scanned — the `data/` dated-artifact naming convention: `probe/cache_ramp.py` line 104 hardcodes its output filename to `cache_ramp_20260905.json`, the **only** hardcoded output filename in the codebase, while every other dated artifact derives from the clock ([`probe/run.py:208`](https://github.com/leshchenko1979/inferhub-watch/blob/main/probe/run.py) → `data/runs/<ISO>.json`; [`probe/pricing.py:730`](https://github.com/leshchenko1979/inferhub-watch/blob/main/probe/pricing.py) → `data/pricing/<date>.json`), so a re-run on a later date overwrites a **tracked** record whose name asserts a date the payload does not have. Filed [issue #19](https://github.com/leshchenko1979/inferhub-watch/issues/19), self-approved (premises verified first-hand: sole hardcode, artifact tracked, no consumer reads it by name), created Telegram forum topic 476 (`Worker — #19 cache_ramp dated output`), spawned worker `2bb2e9b9` (`ad6f4b4a-0c26-4614-8621-815bd5016c8e`), session bound, briefed via `session_notify`. `data/runs/` growth was examined in the same scan and deliberately **NOT** proposed — those files are load-bearing history (`probe/pricing.py:416` rebuilds sweep windows from them, `probe/radar.py:29` reads the newest, `scripts/sync_usage_logs.py:54` flags rows inside known probe-run windows).

## Archived improvement-ledger entries (2026-09-12)

- 2026-09-12 04:0xZ hourly cycle — cycle continuity clean. Surface health verified live: Grafana 24/24 panels valid via `test_grafana_panels.py` (0 errors), Pages 200 ok (122.4 KB), route_metrics updated 03:24:30Z with 115 live catalog routes synced. Worker receipt from lane `1122b15e` processed and verified: commit `540f435` landed on origin/main, 572 tests pass. Both #25 (hourly catalog price query) and #21 (worker brief delivery & title renaming) verified closed with live receipts. Board down to 1 open issue (#18). Switcher dry-run: `cb/deepseek-v4.1-flash` holds top crown (Val=405,907.6, TPS=240.1 [prod], catalog_source=live_api). Scan object: Class 2 Data & Storage — upgraded from IMPROVING to HEALTHY following #25 landing (hourly catalog floor asks now refreshed alongside Postgres usage logs).

- 2026-09-12 05:0xZ hourly cycle — cycle continuity clean. Surface health verified live: Grafana 24/24 panels valid via `test_grafana_panels.py` (0 errors), Pages 200 ok (122.4 KB), route_metrics updated 04:24:28Z with 115 live catalog routes synced. Switcher dry-run: `cb/deepseek-v4.1-flash` holds top crown (Val=405,907.6, TPS=240.1 [prod], catalog_source=live_api). Board: 1 open issue (#18, awaiting ~07:19Z scheduled sweep). Cleanliness: tree clean, 572 tests pass in 7.11s. Scan object: Class 3 Surfaces — confirmed zero panel query regressions across both stat rows and timeseries.

- 2026-09-12 06:0xZ hourly cycle — cycle continuity clean. Surface health verified live: Grafana 24/24 panels valid via `test_grafana_panels.py` (0 errors), Pages 200 ok (122.4 KB), route_metrics updated 05:24:25Z with 123 live catalog routes synced. Switcher dry-run: `cb/deepseek-v4.1-flash` holds top crown (Val=405,907.6, TPS=240.1 [prod], catalog_source=live_api). Board: 1 open issue (#18, awaiting ~07:19Z scheduled sweep). Cleanliness: tree clean, 572 tests pass in 7.00s. Scan object: Class 5 Code & Config — verified candidate valuation pipeline, TPS power-law weighting ($w=0.50$), and Postgres live overlay integration.

- 2026-09-12 07:0xZ hourly cycle (extra trigger 07:04Z, 4 min after the 07:00Z cycle — deduped, clock gate re-verified live) — **MATERIAL SIGNAL ELEVATED: switcher dry-run says `should_switch: true`** — `ag/gemini-3.8-flash-high` (Val=86731.9, TPS=342.9[prod]) exceeds incumbent `cb/deepseek-v4.1-flash` (Val=50050.6, TPS=240.1[prod]) by **73.3%** (threshold 15%, exceeded 4.9×); signal live since ≥07:00Z cycle. **Gap found & filed [issue #26](https://github.com/leshchenko1979/inferhub-watch/issues/26): no production executor armed** — `cron_manage list` (17 jobs), system crontabs on agents/apps/vpn, `/etc/cron.d`, systemd timers all carry NO `auto_route_switch` entry; the ONTOLOGY swap rule (">15% → swap") has never been armed, HQ cycle is dry-run-only. Spend delta computed in python from receipted inputs (first pass used hand-typed 0.15 instead of the receipted 0.000150 — unit-scale catch, redone): fleet 24h 23,313 reqs / 1.774B in / 14.75M out toks, actual billed $0.5847; incumbent floor $0.275/24h ($8.25/mo) vs challenger $1.386/24h ($41.58/mo) = **+$33/mo floor, ~+$70-88/mo at realized ×2.13 ratio** for +73.3% Value. Config verified live: fleet default `cb/deepseek-v4.1-flash` (config.toml:81,:338-339), `subagent_model = ag/gemini-3.8-flash-high` (:347) — challenger already serves subagents. Executor decision = owner gate (spend); filed, not self-executed. Surfaces: sweep window opens ~07:19Z (no scheduled run yet at 07:04Z), Grafana 24/24 panels valid, route_metrics 06:24:23Z, Pages 200. Scan object: Class 4 Schedulers & Triggers.
- 2026-09-12 07:15Z **LIVE-FIRE SWITCH EXECUTED** (cron `fef19c4f` test-fire, armed 07:13Z per owner order): fleet default switched `cb/deepseek-v4.1-flash` (IQ=39.5, In=$0.00450, Out=$0.01800, Val=13,251.7) → `ag/gemini-3.8-flash-high` (IQ=41.2, In=$0.00825, Out=$0.04125, Val=87,718.6), **+561.9%** at realized asks; **1,294 active sessions migrated**; config.toml default updated (verified live post-switch). Executor law holds: sole production authority = cron :33 hourly; HQ stays dry-run-only. #26 closed with receipt. Next: 08:00Z cycle verifies no thrash + crown stability.

- 2026-09-12 07:2xZ **PARTLY RETRACTED - see the 10:0xZ entry: disabling cron fef19c4f did NOT stop switching. scripts/sync_usage_logs.py:349 calls the switcher in PRODUCTION on every hourly sync and had been armed since 2f8f32c (2026-09-10).** **THRASH INCIDENT — switcher PAUSED (HQ mitigation, cron `fef19c4f` enabled=false at ~07:26Z)**: second live-fire flip ~07:24Z reverted the fleet to `cb/deepseek-v4.1-flash` (Val=763,522.4) after the 07:15Z flip to `ag/gemini-3.8-flash-high` — **2 flips in ~11 min, 1,294 sessions migrated each, cache warmth reset twice**. Root cause = valuation basis instability, receipted: same model, same IQ (39.5), same TPS (240.1 [prod]) — Val swung 405,907.6 (07:00Z) → 50,050.6 (07:04Z) → 13,251.7 (07:15Z, realized In=$0.00450) → 763,522.4 (07:25Z, identical displayed asks) = **58× swing** from realized-vs-floor basis flipping as traffic moves; gemini-3.8's displayed ask also flipped 11× ($0.00825 → $0.00075) across the 07:23Z sync. The 15% threshold is meaningless against basis noise — the '+561.9%'/'+214.2%' gains were basis artifacts, not value deltas. Deepseek realized In=$0.00450 vs its own floor ~$0.00015 (30×) flagged for a realized-data sanity check. Filed [issue #27](https://github.com/leshchenko1979/inferhub-watch/issues/27) with the full receipt table; dispatched to persistent lane `1122b15e` same-turn (`session_notify`; the 'no wake in 10s' verdict was slow pickup — `session_search` re-check showed the lane RUNNING at 07:27:48Z, 68 msgs). Fix scope: single basis for all candidates, 6h dwell + >3x override, pinned floor-point selection in the catalog sync, realized-data sanity check. Re-arm criteria: all #27 items landed + dry-run stability across two consecutive synthetic basis shifts. Next materialization risk was 08:33Z (incumbent re-accumulates realized traffic → flip #3); 07:33Z natural run would have NOOP'd (gain 0.0%, verified dry-run).

**Archived 2026-09-12 14:0xZ** (evicted by the D1–D7 owner-ruling entry; retention law 10-entry cap):
- 2026-09-12 07:5xZ **HQ verified the #27 fix (lane 1122b15e receipt processed 07:54Z) + #18 CLOSED + #28 filed**: commits f4d08f4 + 42f90a6 on origin/main (git log verified; tree clean except pre-existing untracked `data/qual_runs.json`), 612 tests pass (HQ-run, was 572), comment 5644582005 verified via gh api; live dry-run: `should_switch=false`, `basis_mode=floor`, gain 0.0%, dwell=`no_prior_switch` (`data/switcher_state.json` not yet created — both incident flips predate the state file, so the post-re-arm first switch is dwell-open; noted, no action). Fix legs verified: single basis per comparison, 6h dwell + >3x override, pinned floor-point (`probe/floor.py`, both selectors delegate), realized-ask verdict = DEFECTIVE placeholder → filed [#28](https://github.com/leshchenko1979/inferhub-watch/issues/28) (upstream question, not corrected locally). Re-arm scoreboard (SKILL.md:67): (1) ✅ landed+612 tests, (3) ✅ floor receipt `0.0045@1p` = real filled ladder point (matches 07:15Z notification), (2) ⏳ synthetic basis-shift stability harness dispatched to lane `1122b15e`, (4) owner report this turn — **cron `fef19c4f` stays [disabled] per the owner-gated re-arm law; verified live in cron list**. BONUS closure: [#18](https://github.com/leshchenko1979/inferhub-watch/issues/18) CLOSED — scheduled run [34680077916](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34680077916) fired 07:11:32Z rendering `Watch — sweep` (criterion 3 receipt). Board: #18 closed; open = [#28](https://github.com/leshchenko1979/inferhub-watch/issues/28) (upstream-gated) only.

**Archived 2026-09-12 14:1xZ** (evicted by the item-2 gate-scoring entry; retention law 10-entry cap):
- 2026-09-12 07:5xZ **#27 FIX LANDED** (lane `1122b15e`; code commit [`f4d08f4`](https://github.com/leshchenko1979/inferhub-watch/commit/f4d08f4739d140b747b12966bba70d1fe8f99ea9) on `main`, **612 tests pass** — was 572). All four #27 items landed: (1) **single floor-point owner** — new `probe/floor.py` owns the ladder rule (cheapest point with `count >= 1`, ties to the larger publisher count); `probe/pricing.py` and `probe/catalog.py` both delegate, so `data/pricing.json` and `route_metrics`/switcher can no longer publish two different floors for one route on one pull. The old `catalog.fetch_models` bare `min()` ignored the publisher count and could select an advertised-but-unfilled point — that is the phantom "$0.00015 floor" of the incident entry: not a ladder point at all. `floor_pair_detail()` keeps a price and its publisher count on **one** selection so they can never be read from different points. (2) **basis consistency** — `select_basis()` returns the realized basis only when **every** candidate carries ≥5 realized samples, `apply_basis()` then ranks all candidates on that one basis (floor by default), and `find_best_route` admits the incumbent to the basis pool and reports `basis_mode`/`basis_samples`; a partially-populated realized set can no longer be ranked against a floor-basis challenger — the exact mechanism of the 07:15Z/07:24Z pair. (3) **dwell gate** — `DWELL_SECONDS=6h` + `DWELL_OVERRIDE_GAIN=3.0`, state in `data/switcher_state.json` (gitignored), written **before** the notify so a failed alert cannot lose dwell state; missing/corrupt state leaves the gate **open** (a bad file must never block a legitimate switch); `--dwell-hours` / `--override-gain` CLI flags. (4) **floor-point receipt** — `sync_route_metrics` logs one `floor point <route>: in=<price>@<n>p out=<price>@<n>p` line per route per pull and flags `<== FLOOR FLIP from <old> (<ratio>x)` against the stored row, so a floor move is visible on the pull that causes it (live dry run: 115 routes → 115 lines). **Realized-ask sanity verdict (item 4): DEFECTIVE** — `cb/deepseek-v4.1-flash` bills **9,310 of 9,424** requests (98.79%) at exactly `0.000150` in / `0.000600` out, i.e. exactly `official_in/1000` and `official_out/1000` to 6 dp; the value appears **nowhere** in the route's live ladder (cheapest filled `0.004500@1p`), so it sits 9–30× below every offer a publisher makes; only 3 of 11 routes carry the exact signature (this route + both `ag/gemini-3.*-flash-high`), so it is not a global unit convention. Not corrected locally (that would be guessing the upstream unit) — follow-up with InferHub. Evidence: [`reports/2026-09-12-issue-27-realized-ask-verdict.md`](https://github.com/leshchenko1979/inferhub-watch/blob/main/reports/2026-09-12-issue-27-realized-ask-verdict.md). **Switcher remains PAUSED** (cron `fef19c4f` disabled since ~07:26Z). Oldest ledger entry (2026-09-11 09:4xZ owner ruling "1+3") archived to WORKLOG.md per the retention law.

- [archived from SKILL.md Improvement ledger 2026-09-12 14:4xZ per retention law]
- 2026-09-12 08:1xZ **RE-ARM CRITERION 2 MET — synthetic basis-shift stability harness** (lane `1122b15e`, HQ dispatch 08:00Z): new [`scripts/basis_shift_harness.py`](https://github.com/leshchenko1979/inferhub-watch/blob/main/scripts/basis_shift_harness.py) + 13 tests ([`tests/test_switcher_basis_shifts.py`](https://github.com/leshchenko1979/inferhub-watch/blob/main/tests/test_switcher_basis_shifts.py)), **625 tests pass** (was 612). It drives the REAL entry point `run_auto_route_switch` over a temp root (fixture `catalog.json`/`intelligence.json`/`pricing.json`/`config.toml`) with `dry_run=True`, `notify=False`, `pgstore.window_rows` + `resolve_inferhub_key` patched offline, and every write surface (`update_opencrabs_config`, `update_active_sessions`, `send_session_notification`, `resolve_notify_session`) patched to **raise** — zero production mutation, MEASURED not asserted: all four mock call counts 0, live `config.toml` bytes unchanged, no `data/switcher_state.json` in the repo, `catalog_source=data/catalog.json` on every run (offline proof). Fixture = the incident pair on the incident shape: incumbent `cb/deepseek-v4.1-flash` floor 0.00040 / realized 0.00090, challenger `ag/gemini-3.8-flash-high` floor 0.00060 / realized 0.00120. **Basis forced twice: floor → realized → floor.** Verdict line `[criterion-2] shifts=2 stable=True non_vacuous=True mixed_would_switch=True -> PASS`. **Shift 1** (floor→realized, challenger at 5 samples): `should_switch: false`, `basis_mode=realized`, `basis_samples=5`, Val=42222.2, dwell=`no_prior_switch`, ranked on ONE basis — incumbent 42222.2 vs challenger 31666.7 (**−25.0 %**, challenger loses). **Shift 2** (realized→floor, challenger at 0 samples): `should_switch: false`, `basis_mode=floor`, Val=95000.0, ranked — incumbent 95000.0 vs challenger 63333.3 (**−33.3 %**, challenger loses). The gate is load-bearing, not vacuous: the challenger is qualified on every run, the dwell gate is OPEN (`no_prior_switch`), and the pre-#27 **MIXED** ranking on the same fixture (incumbent on realized 0.00090 vs challenger on floor 0.00060) reads **+50.0 %** → `would_switch: true`. **Naming finding (Val reconciliation):** the dry-run's `Val=763900.3` on `basis=floor` is honest, not a bug — `resolve_floor_price()` returns `basis.projected(...)` whenever the crown gate passes, so `basis=floor` names the **traffic-independent basis family** (projected when the gate passes, catalog floor ask otherwise), never billed history; live projected `4.6569931072098897e-05` vs catalog ladder floor eff `0.004635` (99.5× apart), recomputed Val **764089.0** = 0.0247 % from the dry-run's 763900.3 (live TPS drift 240.5→240.7). Consistency holds because `apply_basis()` sets `eff_price=floor_price` for EVERY candidate. **Re-arm scoreboard: 3/4** — (1) ✅ landed + 625 tests, (2) ✅ this harness, (3) ✅ floor receipt `0.0045@1p`, (4) ⏳ owner report (HQ-side). **cron `fef19c4f` stays disabled** — owner-gated. Ledger was at 11 entries (one over the 10-cap); the two oldest (2026-09-12 03:0xZ and 2026-09-11 10:0xZ cycles) archived to WORKLOG.md in this same edit.

## Archived improvement-ledger entries — recovered from the deleted repo-root WORKLOG (2026-09-12)

**Why this section exists.** A SECOND tracked archive lived at the repo root (`WORKLOG.md`). The retention law
names only `skills/inferhub/WORKLOG.md`, yet the 11:14:16Z repair (commit `12840df`, "… + restore 24 dropped
archives") appended 35 lines to the root path and never touched the canonical one — so the two files split, and
neither was a superset of the other. Every root entry whose content is absent from this file is consolidated
here **verbatim** (its `[rotated out …]` provenance prefix kept), and the root file is deleted in the same
commit, so a bare `WORKLOG.md` reference can no longer resolve to a second archive.
[issue #40](https://github.com/leshchenko1979/inferhub-watch/issues/40)

**Recovery provenance — the deleted file's own preamble, preserved.** Rotated out of `SKILL.md` per the ledger
retention law ([issue #5](https://github.com/leshchenko1979/inferhub-watch/issues/5)). One block was that
cycle's rotation; the rest were rotated out but NEVER appended — recovered from `SKILL.md` git history and
restored. That is why this archive holds entries whose rotation-out timestamps do not match the section they
sit under.

**One claim below is RETRACTED, and is preserved as history, never as current fact.** The 2026-09-12 07:1xZ
entry records the `inferhub-auto-switcher` cron as "the sole production switching authority". The live
authority is the hourly sync cron
([`scripts/sync_usage_logs.py:349-350`](https://github.com/leshchenko1979/inferhub-watch/blob/main/scripts/sync_usage_logs.py)),
and `fef19c4f` is `enabled=false` and redundant — see the 2026-09-12 10:0xZ entry above and SKILL.md
§Armed auto-switcher.

### 2026-09-10 (recovered from the repo-root archive)

- [rotated out 2026-09-10 18:05:40 +0000] - 2026-09-10 07:00Z hourly cycle — sweep healthy (schedule run success 2026-09-09T07:25Z, 23.6h, within window; GitHub has not emitted the 02:17Z firing for 09-10 — three cycles watching; if 07:17Z passes silent, it's a real cron gap → ops: issue), Grafana 200 ok + probe data fresh (route_metrics max updated_at 06:24Z, minutes old — new leg (b) works), site 200, board 0 open, hygiene clean. Duplicate cron notify investigated: single job, test-fire + scheduled fire landed minutes apart — no duplicate. Scan object: the Improvement ledger itself (unbounded growth at hourly cadence) — [issue #5](https://github.com/leshchenko1979/inferhub-watch/issues/5) filed, self-approved, retention law landed same cycle (10-entry cap, overflow archived to WORKLOG.md; 4 oldest entries archived this landing). Landed.
- [rotated out 2026-09-10 18:05:40 +0000] - 2026-09-10 08:00Z hourly cycle — sweep healthy ([run 34449518157](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34449518157) completed success at 07:21Z, fresh), Grafana 200 ok + probe data fresh (updated_at 07:27Z). Board: 1 open ([issue #6](https://github.com/leshchenko1979/inferhub-watch/issues/6)). Rules landed: subagents strictly for review tasks (fresh context required); cron trigger updated to `delivery: mode: quiet`. Scan object: cron notify noise & subagent role boundary. Landed.
- [rotated out 2026-09-10 18:05:40 +0000] - 2026-09-10 08:45Z: silent cron trigger law landed (owner order: "This can be silent") — cleared `deliver_to` on `inferhub-watch-hq-hourly` so the thin trigger job no longer dumps an execution card into Telegram; only HQ delivers the real cycle report. Landed.
- [rotated out 2026-09-10 18:05:40 +0000] - 2026-09-10 09:12Z: review research law landed (owner order: "When reviewing, research web and accumulate best practices for different aspects that aroused your suspicion") — review lanes must research suspicious patterns on the web and cite empirical best practices across code, auction math, Grafana, and agent skills. Landed.
- [rotated out 2026-09-10 18:05:40 +0000] - 2026-09-10 10:30Z hourly cycle — sweep healthy ([run 34449518157](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34449518157)), Grafana 200 ok (public dashboard token verified live with full data), probe data fresh (10:24Z). Board 0 open, 481 tests pass. Scan object: tracked canonical Grafana dashboard JSON in repo `dashboards/watch.json` ([issue #10](https://github.com/leshchenko1979/inferhub-watch/issues/10)), self-approved and landed in commit https://github.com/leshchenko1979/inferhub-watch/commit/252542a.

### 2026-09-12 (recovered from the repo-root archive)

- 2026-09-12 07:1xZ owner order "Arm hourly auto-switcher" EXECUTED same-turn — cron `inferhub-auto-switcher` created (id `fef19c4f-ab16-442e-8ee9-e041d3d0919b`, `33 * * * *` UTC, deliver none, pattern mirrors `inferhub-usage-logs-sync`: thin prompt, production `auto_route_switch.py`, script-owned config writes + its own Telegram switch alert, NOOP = silent, 🔴 escalation to owner topic on failure), **test-fired 07:1xZ for end-to-end validation** (first natural run 07:33Z); [#26](https://github.com/leshchenko1979/inferhub-watch/issues/26) closes. Delegation law gained the **Armed auto-switcher** bullet — the cron is the sole production switching authority, HQ cycles stay dry-run-only. Signal at arming: `should_switch: true`, +76.2% (deepseek-v4.1 → gemini-3.8-flash-high). Class 4 Schedulers: 17 → 18 crons.

- [archived from SKILL.md Improvement ledger 2026-09-12 14:4xZ per retention law]
- 2026-09-12 10:0xZ **CORRECTION - the auto-switcher has been LIVE HOURLY since 2026-09-10 (issue #26's "no executor" finding AND the 07:2xZ "switcher PAUSED" entry are both RETRACTED)** - [`scripts/sync_usage_logs.py:349-350`](https://github.com/leshchenko1979/inferhub-watch/blob/main/scripts/sync_usage_logs.py) calls `run_auto_route_switch(catalog_models=live_models)` with **no `dry_run` argument** -> **PRODUCTION**, in-process, on every hourly sync (cron `2d9112a6-b697-4eeb-b6ac-3e2462e23483`, `23 * * * *` UTC); added in [`2f8f32c`](https://github.com/leshchenko1979/inferhub-watch/commit/2f8f32c) (2026-09-10 14:44:24 +0000). Receipts: 8 consecutive sync runs all `success` through 09:23:29Z; `data/qual_runs.json` mtime 2026-09-12 05:24:21Z - a file only a NON-dry-run switcher can write (`auto_route_switch.py:575` -> `probe/tps_qual.py:129` `save_qual_runs()`, reached only when `client is not None`, which requires `not dry_run`); `data/switcher_state.json` ABSENT -> no switch has fired since the #27 fix landed. So disabling `fef19c4f` at ~07:26Z bought nothing. **FINDING A (dispatched to lane `1122b15e` with a fix):** [`probe/pricing.py:331`](https://github.com/leshchenko1979/inferhub-watch/blob/main/probe/pricing.py) `round(eff, 4)` is an **absolute** 4-decimal snap -> 1-2 significant figures at these magnitudes; recomputed live from `data/pricing.json`, **14/14** stored asks sit exactly on the 0.0001 lattice. It MANUFACTURES the tie: `cb/deepseek-v4.1-flash` raw eff 0.00035302 and `ag/gemini-3.8-flash-high` raw eff 0.00038408 - a real 8.80% gap - both store as `0.0004`; the board sorts `(eff_val, -iqps)` (`site/board.py:154`), so on an equal ask the higher IQ/$ wins and the board leads with the DEARER route. It reaches the switcher through `probe/basis.py:27` -> `scripts/auto_route_switch.py:226` - a decision input, not display. (An earlier draft cited a 6.13% gap from a pre-compaction reconstruction that does not reproduce; corrected to the live pair, and the correction was sent to the lane.) **FINDING B (analysed, HELD - owner-gated):** the snapshot declares `range: 30d` but carries `requests_scanned: 12000` = `MAX_PAGES(120) x PAGE_SIZE(100)`; rows span **11h05m** (Sep 11 20:08 -> Sep 12 07:13), i.e. 64.96x short of 30d (true 30d = 80,751 rows -> 14.86% captured), because the builder is GitHub Actions `ubuntu-latest` (`.github/workflows/watch.yml`, `probe` job) with no route to the tunnel `127.0.0.1:15432`, so `probe/pgstore.py` is never used (all 12 committed snapshots read `row_source: api`); the fix route is infra/spend -> owner. **Class 1 scan:** `data/qual_runs.json` untracked AND unignored (`git check-ignore` rc=1) -> `.gitignore`. **Cycle continuity:** no ledger entry for the 08:00Z/09:00Z cycles — those hours ran owner-directed work (the methodology thread and the #27 findings filing) instead of cycles; recorded so the next continuity check reads the gap as displaced, not missed. Owner asked for three calls: (1) keep the live hourly switcher on? (2) #27-B fix route - CI credential or build on this box? (3) feed `projected()` the hourly **catalog** ask instead of the billed ask, which is scaled /30 on deepseek and /11 on gemini-3.8 non-uniformly.

**Archived 2026-09-12 16:18Z** (evicted by the "WE PRICE THE BOOK" entry; retention law 10-entry cap):

- 2026-09-12 10:3xZ **OWNER DECISION "Keep switcher live" - the hourly sync-cron switcher STAYS ARMED; the first post-#27 PRODUCTION evaluation was a NOOP.** Answering call (1) of the 10:00Z report, the owner ruled the live switcher stays on. Nothing had to be enabled: the sync path was already armed (that was the correction). **Receipt - sync cron run `2026-09-12T10:23:14Z`, `success`** (first production evaluation on the #27-fixed code). NOOP, established by SIDE-EFFECTS (the run's own stdout line is NOT recoverable - the cron session's stored tool output is truncated at 3,180 chars and `cron_job_runs.content` carries only the sync summary, 2,593 rows upserted / freshness `2026-09-12 10:23:41.545+00` / 85,177 rows total, with no switcher line): (a) `config.toml` fleet default still `cb/deepseek-v4.1-flash` (:81 fleet, :339 active, :347 subagent); (b) `data/switcher_state.json` ABSENT - a switch writes it BEFORE the notify; (c) `data/qual_runs.json` mtime unchanged `2026-09-12 05:24:21Z` - no probe ran. Live HQ dry-run 10:30Z agrees: `should_switch: false`, gain 0.0%, Val=765686.2, TPS=242.4[prod], `basis_mode=floor`, `basis_samples=0`, dwell open (`no_prior_switch`), `catalog_source=live_api`. **Law text corrected:** SKILL.md §Armed auto-switcher no longer calls cron `fef19c4f` the "SOLE production switching authority" - it documents BOTH production paths (the sync cron at :23 = the LIVE authority, armed since `2f8f32c`; `fef19c4f` at :33 = `enabled=false` and now redundant with it). **Still owner-gated:** call (2) the #27-B snapshot fix route (CI Postgres credential vs build on this box) and call (3) whether `projected()` reads the hourly CATALOG ask instead of the billed ask — **framing corrected at 11:0xZ: not a discount pair, two different measurements of different moments; see the 11:0xZ ledger entry.** **Worker lane `1122b15e`** is mid-turn on finding A (`probe/pricing.py:331` `round(eff, 4)` quantization): the file is modified in the shared checkout and the lane's receipt script `scripts/ask_precision_receipt.py` is present - HQ touched NEITHER (shared-checkout law: workers stage only their own files). `.gitignore` for `data/qual_runs.json` still open with the lane (`git check-ignore` rc=1).

**Archived 2026-09-12 17:00Z** (evicted by the "CHURN-METRIC CORRECTION + #36 + #37" entry; retention law 10-entry cap):

- 2026-09-12 10:4xZ **#27 FINDING A FIXED — significant-figure quantization on the realized ask** (lane `1122b15e`; commit [`bd12543`](https://github.com/leshchenko1979/inferhub-watch/commit/bd125432a70d30101c9ca266cd64d0fc9d0cbfd5), **651 tests pass** — was 625). HQ's Finding A confirmed and closed. [`probe/pricing.py`](https://github.com/leshchenko1979/inferhub-watch/blob/main/probe/pricing.py) `round(eff, 4)` was an **absolute** 4-decimal snap — the step stays 1e-4 whatever the magnitude, which across the board's 1e-4..1e-1 ask range leaves one or two significant figures. Replaced at BOTH decision call sites (`eff_per_mtok`, `marginal_per_mtok`) by `sig_round()` with `EFF_SIG_FIGS = 6`, a magnitude-scaling quantizer whose quantum is always finer than the source. **Measured on the committed snapshot, not asserted:** stored asks sitting exactly on the 1e-4 lattice **14/14 → 0/14**; COLLAPSES (distinct raw asks sharing one stored value) **1 → none**; the manufactured tie is gone — `cb/deepseek-v4.1-flash` raw 0.000353017 vs `ag/gemini-3.8-flash-high` raw 0.000384080, a real **8.09 %** gap measured from the snapshot's own cost/token inputs (HQ's corrected live pair reads **8.80 %**; both cited, they differ by reconstruction — the 6.13 % figure was HQ's own error and is discarded) — both stored as `0.0004`; and the board's `(eff_val, -iqps)` sort now leads with the **genuinely cheaper** route instead of the dearer one. Source-precision check: the material routes carry **4.5–5.8** real significant figures of precision off the artifact's own 6-dp cost rendering, so six figures is information-preserving and can never re-create a tie the source resolves (worst case `cbcn/deepseek-v4.1-flash` at 2.3 figures is not a material route). New [`tests/test_ask_precision.py`](https://github.com/leshchenko1979/inferhub-watch/blob/main/tests/test_ask_precision.py) (26 tests) + [`scripts/ask_precision_receipt.py`](https://github.com/leshchenko1979/inferhub-watch/blob/main/scripts/ask_precision_receipt.py) (replays the snapshot's own raw inputs through the real `route_entry`; raw vs stored vs err, lattice count, collapses, board order, dry-run switcher leg; `--json`/`--no-switcher`; exit 1 on a collapse; zero production mutation). **PROVEN to catch the defect:** reverting ONLY the two call sites → **5 failed / 21 passed**, the board test failing on `assert 1 < 0` with `ag/gemini-3.8-flash-high` at index 0; fix restored, md5 `f95f87637a75433c5c0aa76d29014734` identical before and after. **Schema verdict — `cost_usdc numeric(12,6)` is ADEQUATE, no change:** its single-row worst case is 5.0 % of p50, but the decision value is a SUM over rows, so `5e-7/(sqrt(n)·p50)` = **0.0434 %** at n=13,291 — far below the **14.1635 %** the old output snap imposed on `cb/deepseek-v4.1-flash`; a finer scale cannot recover information the API never sent. One real, tiny loss found and REPORTED, not fixed (schema change is owner-gated): **28 rows** carry `cost_usdc = 0` with `tokens > 0` (a true cost below the 5e-7 rounding threshold) — `ag/gemini-3.8-flash-high` loses 947,103 tokens (10 rows) → **0.1432 %** understatement, `ag/gemini-3.7-flash-high` 842,359 tokens (17 rows) → **0.0777 %**. **Pre-existing display-only defect reported, not touched:** under the old snap every *projected* label on the board rendered `$0.0000` (projected values sit at 1.8e-4..4.7e-5); my diff touched `probe/pricing.py` only. **Switcher leg** (real `run_auto_route_switch`, `dry_run=True`, `notify=False`, every live surface patched to RAISE): `should_switch: false`, `basis_mode=floor`, gain 0.0 %, dwell `no_prior_switch`, `live_config_unchanged: true`, realized prices read at full precision. **Live-switcher contradiction acknowledged:** [`scripts/sync_usage_logs.py`](https://github.com/leshchenko1979/inferhub-watch/blob/main/scripts/sync_usage_logs.py) calls the switcher in PRODUCTION on every hourly sync (cron `2d9112a6`, `23 * * * *`), so disabling `fef19c4f` bought nothing — the **dwell gate is the live production guard** and `data/switcher_state.json` is production state. **Dwell-gate coverage CONFIRMED already pinned** — no new test needed: [`tests/test_switcher_basis.py`](https://github.com/leshchenko1979/inferhub-watch/blob/main/tests/test_switcher_basis.py) `TestDwellGate` covers open-on-absent (`test_no_prior_switch_is_allowed`), open-on-corrupt (`test_corrupt_state_never_blocks_a_switch`), open-on-unparseable-timestamp (`test_unparseable_timestamp_never_blocks_a_switch`), plus dwell-active / override-gain / elapsed / state-roundtrip. **Hygiene:** `data/qual_runs.json` added to `.gitignore` beside `data/switcher_state.json` (now line 14; `git check-ignore` rc=0). **Finding B (the declared 30d window is really ~11h): analysis only, no code — owner-gated.**

**Archived 2026-09-12 17:1xZ** (evicted by the "#44 log window + always() probe chain" entry; retention law 10-entry cap):

- 2026-09-12 10:5xZ **OWNER DECISION "CI credential for PG" — #27 Finding B fix route = give the workflow a Postgres credential (NOT build the snapshot on this box); DISPATCHED to persistent lane `1122b15e`.** Answering call (2) of the 10:00Z report. **The defect:** the snapshot declares `range: 30d` but the builder runs on GitHub Actions `ubuntu-latest` ([`.github/workflows/watch.yml`](https://github.com/leshchenko1979/inferhub-watch/blob/main/.github/workflows/watch.yml), `probe` job) with no route to the tunnel `127.0.0.1:15432`, so `probe/pgstore.py` is never used and all 12 committed snapshots read `row_source: api` capped at `MAX_PAGES(120) x PAGE_SIZE(100)` = 12,000 rows. **Reachability verified first-hand BEFORE dispatching** (the whole route is void if a runner cannot open the tunnel): `ssh apps` → `ufw status` = **inactive**, sshd on `0.0.0.0:22`, and the postgres container publishes `0.0.0.0:5432->5432/tcp` — so the tunnel target exists and is listening on a public interface. That establishes the **target**, not the end-to-end CI path: a GitHub-hosted runner actually opening that tunnel is the lane's acceptance criterion, not a HQ claim. **Brief carried to the lane** (`session_notify`, `delivery.mode=turn-end`, `confirm: true` — read back from the daemon log's `[TOOL_ACCUM]` tail for that call id; the lane was mid-turn on finding A; lane receipted "Finding B dispatch received; starting it now"): a read-only `inferhub_ci` role (SELECT on `usage_logs` only), a dedicated CI SSH key restricted with `restrict,permitopen="127.0.0.1:5432"` + a forced command refusing a shell, the five secrets via `gh secret set`, a workflow step opening its OWN tunnel (never the manual pid 3259768 process), the builder preferring `pgstore.window_rows(30*24)` and stamping `row_source: pg` + the TRUE window + rows scanned, and **FAIL LOUD on any PG failure while creds are present** — the API path survives only as the no-PG-env fallback, stamping `row_source: api` + the short real window. Acceptance: unittest count, a local builder run over the tunnel ≈ 80,751+ rows with `row_source: pg`, Grafana 24/24, and a real `workflow_dispatch` run URL showing the tunnel + `row_source: pg` + the row count. One commit per concern; nothing to `gh-pages`. **Finding A verified first-hand by HQ on the committed tree** (lane receipt [comment 5645419635](https://github.com/leshchenko1979/inferhub-watch/issues/27#issuecomment-5645419635), id/author/date/len all re-read via `gh api`: 7,241 chars, `leshchenko1979`, 10:49:25Z; commits [`bd12543`](https://github.com/leshchenko1979/inferhub-watch/commit/bd125432a70d30101c9ca266cd64d0fc9d0cbfd5) + [`c65e8ee`](https://github.com/leshchenko1979/inferhub-watch/commit/c65e8ee8ddf5f4519eee4c145ff66c99b6467338); HEAD == origin/main == `c65e8ee`; tree clean). **Test-count disambiguation (both runners re-run by HQ this turn, both rc=0):** the lane's **651** is `python3 -m pytest tests/ -q` → `651 passed in 10.16s`; CI's own gate is `python3 -m unittest discover -s . -q` ([`watch.yml:46`](https://github.com/leshchenko1979/inferhub-watch/blob/main/.github/workflows/watch.yml)) → `Ran 567 tests in 5.256s` / `OK`. Same suite, two runners — **cite the runner with the count**, never one number alone. **Still owner-gated:** call (3) — whether `projected()` reads the hourly CATALOG ask instead of the billed ask. **FRAMING CORRECTED at 11:0xZ: that is NOT a discount pair — billed and catalog are two different MEASUREMENTS of different moments; see the 11:0xZ ledger entry above for the receipts.** **Finding B live-measured by the lane this turn:** 85,177 rows in 30 d; the 12,000-row cap spans **11.81 h = 1.64 %** of the declared 30 d and is **14.09 %** of true 30 d rows (HQ's 80,751 → 14.86 % / 11 h 05 m was true at its moment — the sync keeps adding rows, so both are right on different clocks).

**Archived 2026-09-12 17:32Z** (evicted by the "#45 accumulation vehicle" entry; retention law 10-entry cap):

- 2026-09-12 11:0xZ **CORRECTION — the catalog/billed “÷30 / ÷11 discount” framing was WRONG: they are two different MEASUREMENTS, not one price seen two ways.** Answering the owner's “Explain the catalog/billed question”. **The billed ask is PER-REQUEST and TIME-VARYING** — [`usage_logs.ask_input_per_mtok`](https://github.com/leshchenko1979/inferhub-watch/blob/main/probe/pricing.py) / `ask_output_per_mtok` carry the ask billed on EACH individual request, and the snapshot's single number is a **7-day MEDIAN** (`ASK_MEDIAN_DAYS = 7` at [`probe/pricing.py:41`](https://github.com/leshchenko1979/inferhub-watch/blob/main/probe/pricing.py), computed by `_median_ask()` at `:256-285` — newest 7 d of billed rows, falling back to the whole 30 d window for quiet routes). **Live per-bucket rows for `cb/deepseek-v4.1-flash`** (`is_probe=false`, grouped by ask): ask_in **0.000150 → 9,310 rows, first 2026-09-11 07:31:38, LAST 2026-09-11 23:30:37**; then 0.004500 (2,940 rows from 2026-09-12 07:26:20), 0.003000 (1,824), 0.003750 (356), 0.005850 (315), 0.005250 (53). **The cheap era ENDED 2026-09-11 23:30:37 and the 7-day median is still quoting it — so the median LAGS the live ask.** **The catalog ask is not a stable truth either:** the committed `data/catalog.json` was written by the 07:11:32Z sweep on `afa8d715` (commit [`2ed1df2`](https://github.com/leshchenko1979/inferhub-watch/commit/2ed1df2)) — **BEFORE** the `floor_point` ladder rule landed in [`f4d08f4`](https://github.com/leshchenko1979/inferhub-watch/commit/f4d08f4) (2026-09-12 07:52:10Z), so the committed snapshot PREDATES the rule meant to govern it; a live fetch NOW returns a THIRD set (`cb/deepseek-v4.1-flash` 0.00225, `ag/gemini-3.7-flash-high` 0.00225, `ag/gemini-3.8-flash-high` 0.00075) and `route_metrics` (updated_at 2026-09-12 10:24:16Z) carries a FOURTH (deepseek 0.003000, both gemini 0.000750). **Implied-ask test (decisive):** solving `cost*1e6 = uncached*X + cached*0.1*X + tout*(ask_out/ask_in)*X` per bucket reconciles EXACTLY (1.0000×) for every bucket EXCEPT two — deepseek's 0.000150 era implies **3.2724×** the recorded ask (a second cost component existed; that ask did not drive that cost), and the 0.000750 gemini bucket implies **1.0975× / 1.0969×** (a consistent ~10 % extra, plausibly cache-write). **Ranking impact — the choice FLIPS the crown:** IQ/$ @billed deepseek **848,187 (rank 1)** vs gemini-3.7 217,280 (rank 2); @catalog deepseek 28,273 (rank 2) vs gemini-3.7 **217,280 (rank 1)**. **CONCLUSION / recommendation: do NOT switch the input — fix the NOISE** — the 7-day median lag (`ASK_MEDIAN_DAYS`) and the stale-code catalog snapshot. The switcher's own catalog-source priority chain ([`scripts/auto_route_switch.py:975-1020`](https://github.com/leshchenko1979/inferhub-watch/blob/main/scripts/auto_route_switch.py): `provided` → `postgres_route_metrics` if ≤3600 s → `live_api` → `data/catalog.json`) already prefers fresh sources, so the fix sits UPSTREAM of it. **Process defect found this turn (this ledger's OWN retention law):** rotations have been DROPPING entries instead of archiving them — **7 entries rotated out after `WORKLOG.md` was created (2026-09-12 07:15:53Z) were absent from it**, all 7 recovered from git history and archived this turn; the 17 pre-WORKLOG losses (24 lost in total) were restored too. Root cause: the archive step was done BY HAND each cycle, so an entry could be deleted from SKILL.md without ever being appended — nothing enforced it.

**Archived 2026-09-12 17:45Z** (evicted by the "#45 design-only" entry; retention law 10-entry cap):

- 2026-09-12 11:4xZ **#27 FINDING B LANDED — CI builds the snapshot from Postgres with its own credential** (lane `1122b15e`; commits `47c686b` workflow, `937c6a5` builder+tests, `cdecee8` host-key pin, `0f744f4` test bound; **669 pytest / 584 unittest**). Owner-ruled route ("CI credential for PG"). **Before:** every committed snapshot read `row_source: api`, capped at `MAX_PAGES(120) x PAGE_SIZE(100)` = 12,000 rows = **11.81 h** of data published as `range: 30d`. **After:** `row_source: pg`, `requests_scanned: 86080`, window **299.31 h of 720 declared (41.57 %)** — [run 34690899955](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34690899955) green 11:22:47Z, all 11 probe steps success, snapshot committed by CI as [7a7eb4f](https://github.com/leshchenko1979/inferhub-watch/commit/7a7eb4fa59fff008695f78c3801293b1cb8a4763). The honest window IS the deliverable — the cache holds nothing older than 2026-08-31, so coverage is DECLARED (41.57 %) instead of faked. Credential: role `inferhub_ci` (login, no superuser/createdb/createrole, SELECT on `usage_logs` only — negatives verified live: route_metrics / route_basis / projection_gate all permission-denied) + a dedicated ed25519 key restricted `restrict,port-forwarding,permitopen="127.0.0.1:5432",command="/bin/false"` (so `ssh -i key apps id` → rc=1, empty output) + five `PG_CI_*` secrets; zero credentials in the repo. **Correction to the brief:** `restrict` DISABLES port forwarding (sshd_config(5)), so the literal `restrict,permitopen=…` would have broken the tunnel it existed for — re-enabled explicitly and proven empirically. **Fail loud:** `PgRequiredError` — creds present and the window fails → exit 1, never a silent degrade to the API cap. **The first CI run FAILED** ([34690577170](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34690577170), `Connection timed out`, exit 255) and the cause was our OWN `ssh-keyscan`: three bare pre-auth connections = three fail2ban failures against `maxretry=3`, banning the runner's own egress IP 21 s before the tunnel step — fixed by PINNING the host key in a secret (`cdecee8`), which also removes the TOFU window. **Two of HQ's expected numbers were wrong; both were measured:** rows 85,177 → **84,559** on the local run (`rows_since` excludes the 618 `is_probe` rows by design) and window 720 h → **298.31 h** (no data older than 2026-08-31 exists). **Follow-on defect found and filed: [#29](https://github.com/leshchenko1979/inferhub-watch/issues/29)** — the CI contract gate (`unittest discover`) collects **0** tests from pytest-style files, so **100 of 669 tests never run in CI**; that is exactly why Finding A's precision guard sat broken (its `rel=1e-6` bound is 5x tighter than the 6-significant-figure quantizer's designed 5e-6 worst case — the first PG-backed snapshot broke it on `ali/deepseek-v4-flash-0731`, 4.39e-6) without CI noticing. Fixed in `0f744f4`: assert the designed identity exactly (`stored == sig_round(raw)`, 0 mismatches over 23 rows), assert accuracy only to `SIG_FIG_REL = 1e-5`, and guard that the tolerance can never be tightened back below the design bound.

**Archived 2026-09-12 17:55Z** (evicted by the "#45 basis-preference design" entry; retention law 10-entry cap):

- 2026-09-12 12:1xZ **#29 FIXED — the contract gate runs on the SHIPPING path and collects every suite** (lane `1122b15e`; HQ ruling "FIX IT, both parts" ~11:5xZ; commits [`de147d4`](https://github.com/leshchenko1979/inferhub-watch/commit/de147d4bce297cb3bd13ac8e6496f35946917365) workflow+requirements and [`c89bf04`](https://github.com/leshchenko1979/inferhub-watch/commit/c89bf041fb26cdaa7a778cbc8cc88db9db5a3470) host-path fix; **669 pytest / 584 unittest**, CI now runs BOTH). **The defect:** `validate` was gated `if: github.event_name == 'pull_request'` while this repo ships by direct push — so the gate had never run against a commit that actually landed — and `unittest discover` sees only `TestCase` subclasses, leaving the 100 tests in 5 pytest-style files invisible twice over. **Now:** `validate` runs on push AND pull_request in its OWN job (so a red test is loud without blocking the sweep), with a pytest step beside the unittest one; `requirements.txt` gained pytest + typing_extensions + psycopg2-binary (the last two are import-time needs of suites the gate must collect — without them the gate fails to COLLECT, not to pass). **The gate earned its keep on the first push run:** [34692645237](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34692645237) went RED with 16 failures `unittest discover` could never see — all one shape: a test that never touches production still reaching for the LIVE ops paths, which a runner cannot read. Two causes, both fixed in [`c89bf04`](https://github.com/leshchenko1979/inferhub-watch/commit/c89bf041fb26cdaa7a778cbc8cc88db9db5a3470): (1) `resolve_inferhub_key` stat'd `DEFAULT_CONFIG_PATH` BEFORE checking whether the caller had passed that very path, so every call with its own config probed the live host — and where /root is unreadable `exists()` RAISES PermissionError instead of returning False (CPython 3.12; 3.13+ swallows it, which is why this only surfaces in CI); equality first, existence second. (2) the basis-shift harness fingerprinted the live config with `read_bytes() if exists() else None`; now `_fingerprint()` treats unreadable as "nothing to fingerprint" and the no-mutation proof rests on the write counters; the receipt gains `live_config_readable` and the harness CLI REQUIRES it, so a vacuous comparison can never read as a verified one. **Green:** push run [34692920135](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34692920135) — `validate` success (`669 passed`, `Ran 584 tests`), `pages` deployed, `probe` correctly skipped. **Verified in both environments that matter:** the ops box (root, live config readable → `live_config_readable=True`, bytes unchanged, 0 write calls) and a CI simulation (python3.12 + unprivileged user + unreadable /root, reproducing a runner exactly) → 669 passed / 584 OK. **Sweep unaffected — proven, not argued:** dispatch [34692956779](https://github.com/leshchenko1979/inferhub-watch/actions/runs/34692956779) ran `probe` all-success, committed [`d490b1e`](https://github.com/leshchenko1979/inferhub-watch/commit/d490b1eabe5ea7992321a2c9742611e6f656e139) (`row_source: pg`, 86,080 rows, 299.31 h), and `validate [skipped]` — the gate does not even run on a sweep, so it cannot block one.
