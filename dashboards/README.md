# InferHub Dashboards

> **GENERATED / TEST-FIXTURE ONLY**
> 
> The authoritative deployment source for Grafana dashboards is in `vds-servers`:
> `/root/vds-servers/apps/services/grafana/dashboards/inferhub/watch.json`
>
> `watch.json` in this directory serves as a test fixture for `tests/test_dashboard_basis.py`.
> Any production dashboard changes must be made and deployed from `vds-servers`.
